Playbooks to manage and automate my homelab infrastructure.

## Playbooks

- `update-all.yml` — updates and upgrades all machines
- `node-exporter.yml` — installs and configures node-exporter for prometheus monitoring

## Inventory structure

- `lxc` — Proxmox LXC containers
- `raspberry` — Raspberry Pi node
